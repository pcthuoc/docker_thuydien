services:
  frontend:
    build:
      context: ./repo/FE
      dockerfile: Dockerfile
      args:
        VITE_API_MODE: real
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - nginx

  nginx:
    ports:
      - "8082:8082"
    depends_on:
      - frontend
