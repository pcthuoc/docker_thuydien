services:
  frontend:
    build:
      context: .
      dockerfile: ./fe/Dockerfile
      args:
        VITE_API_MODE: real
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - nginx

  nginx:
    ports:
      - "8082:8082"
    depends_on:
      - frontend
